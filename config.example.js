// Copy this file to `config.local.js` and fill in the values.
// `config.local.js` is gitignored — never commit it.
//
// Once the Vercel backend lands these keys move server-side and this file
// is no longer needed.
window.SAGGY_CONFIG = {
  // Google PageSpeed Insights API key.
  // Generate at: https://console.cloud.google.com/apis/credentials
  // IMPORTANT: restrict by HTTP referrer to your domain(s) before using.
  PSI_API_KEY: 'YOUR_PSI_API_KEY_HERE',
};
